[storage]
driver = "btrfs"
runroot = "/run/containers/storage"
graphroot = "/var/lib/containers/storage"

[storage.options.btrfs]
# Use native btrfs subvolumes for container layers
# Each image layer = btrfs subvolume, containers use snapshots
# No overlay/FUSE overhead - direct copy-on-write at filesystem level
